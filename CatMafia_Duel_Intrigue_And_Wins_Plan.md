# Cat Mafia — план: интрига дуэли + суммы выигрышей

Дата: 2026-09-23 (обновлено: **B/cat + B/dog — SMOOTH · VH**; **симметрия win/lose**)  
Scope: `third_party/web-sdk/apps/cat_mafia` (фронт) + `third_party/math-sdk/games/0_0_cat_mafia` (математика).  
Статус: тема **B** — целевая форма **величины победы** SMOOTH · VH для cat/dog **в обе стороны** (win и lose); реализация math в работе. Тема **A** — после accept B.

Связанные документы: `CatMafia_Duel_SMOOTH_VH_Implementation_Plan.md` (реализация B), `CatMafia_Duel_Bonus_TZ.md`, `MATH_COMMANDS.md`.  
Таблицы what-if: canvas `duel-vh-only`.

---

## 0. Решение по стратегии (2026-09-23)

**Интрига нужна математическая, не UI-подмена цифр.** Игрок видит правдивые банки; азарт — потому что book держит бой близким.

1. Тема A — не theatrical/fake banks.
2. Порядок: **B → A**.
3. UI-полировка лапы — только опц. хвост после math A.

---

## 0.1 Две темы

| # | Тема | Суть | Порядок |
|---|---|---|---|
| **B** | Форма / суммы победы | Убрать крохи и горб; плавный спад от buy вверх; **то же на проигрышах** (победа оппонента) | **1** |
| **A** | Интрига гонки банков | Оба банка копится; реальное сближение `dogTotal`/`catTotal` | **2 — после B** |

---

## 0.15 Симметрия win / lose (зафиксировано 2026-09-23)

Проигрыш = **победа другого** с теми же весами по сумме, не «пустой ноль».

- Если среди wins ~X% дают ~`$1500`, среди loses ~X% должны выглядеть как «оппонент сорвал ~`$1500`».
- Метрика на lose: `victoryAmount = dogTotal + catTotal` (тот же pot, что payout при win); игроку всё равно `0`.
- Hit-rate *кто* победил не трогаем; RTP на lose не платит.
- Конкуренция по пути (оба баланса растут, «чуть не хватило») — фаза **A**; каскад величины — фаза **B** (win **и** lose).

Детали реализации: `CatMafia_Duel_SMOOTH_VH_Implementation_Plan.md` § B.0.1.

---

## 0.2 Зафиксировано по теме B — `bonus_duel_cat` ✅

Согласовано 2026-09-23:

| Параметр | Решение |
|---|---|
| Форма тела | **SMOOTH** — монотонный спад: чем больше ×, тем реже |
| Пик | **`$150–180`** (только что выше buy `150×`) — самый частый **размер победы** |
| Далее | `$180–220` → `$220–280` → `$280–350` → `$350–450` → `$450–550` → `$550–700` (каждый шаг ниже) |
| Победы `<$150` | **VERY HARD → ~2%** среди wins (сейчас ~22%); **то же среди loses** по pot |
| E[win\|win] / RTP | Держать ≈ сейчас (`~$288` / RTP ~0.96) |
| Hit-rate win | ~50% без изменений |
| Хвост `$700+` / soft `$2.5k` / **max `$25k`** | Не режем в what-if; редкость как в publish |
| Горб `$350–450` (сейчас 33%) | Убрать → ~10–11% в каскаде |
| Lose-зеркало | Тот же TARGET-каскад по `victoryAmount` среди `duel_lose` |
| `bonus_duel_dog` | **Тоже SMOOTH · VH** — снять горб `$1k–1.5k` (~19%→~5%), длинный монотонный каскад до `$2.5k–5k` (+ lose-зеркало) |

### Целевой каскад тела (SMOOTH · VH, % среди wins, E≈$288)

| Band | Now | **TARGET** |
|---|---|---|
| `<$150` (сумма) | 21.8% | **2.0%** |
| `$150–180` ★ | 5.3% | **23.7%** |
| `$180–220` | 7.8% | **19.3%** |
| `$220–280` | 10.2% | **15.8%** |
| `$280–350` | 10.1% | **12.9%** |
| `$350–450` | **33.3%** | **10.5%** |
| `$450–550` | 11.1% | **8.6%** |
| `$550–700` | 0.2% | **7.0%** |
| `$700+` / max | 0.21% | **без изменений** |

Почему `$550–700` чуть растёт: при пике на `$150–180` нужна EV-компенсация, иначе средний win просядет. Каскад всё равно монотонный (7% < 8.6% < … < 24%).

### Почему сейчас горб на `$350–450`

Opt `distribution_bias` + scaling залипают тело около mid band при cost `150` — не продуктовый каскад. Цель B — заменить это явной формой SMOOTH.

### Рычаги реализации (когда дойдём до кода)

1. Перестроить `_bonus_duel_cat_scaling` + `distribution_bias` под каскад выше (пик чуть выше cost, спад вверх).
2. При необходимости лёгкий floor / enforce `match_duel_win_body` под целевые доли бендов.
3. M5 → проверка гистограммы vs TARGET → resample → accept.
4. Soft/HARD/EXTREME what-if больше не рабочие цели для cat — рабочий якорь только **SMOOTH · VH (2%)**.

### Чеклист B (cat)

- [x] Замер гистограммы publish LUT
- [x] Выбор формы: SMOOTH · пик `$150–180` · below→2%
- [ ] Правка opt (`game_optimization.py` scaling/bias) под TARGET
- [ ] M5 → сверка каскада / RTP / P(win)
- [ ] Resample + storybook типичных wins
- [ ] **Accept B (cat+dog)** → потом A

---

## 0.3 Зафиксировано по теме B — `bonus_duel_dog` ✅

Тот же принцип, что у cat: VH below→2%, пик над buy, монотонный спад. E[win|win]≈`$574`, P(win)≈25%, max `$25k` rarity без изменений.

### Целевой каскад dog (SMOOTH · VH, % среди wins)

| Band | Now (ориентир) | **TARGET** |
|---|---|---|
| `<$150` | 23.9% | **2.0%** |
| `$150–180` ★ | 16.9% | **16.7%** |
| `$180–220` | 10.6% | **14.3%** |
| `$220–280` | 3.0% | **12.2%** |
| `$280–350` | 2.6% | **10.5%** |
| `$350–450` | 5.2% | **9.0%** |
| `$450–550` | 1.9% | **7.7%** |
| `$550–700` | 5.5% | **6.6%** |
| `$700–1000` | 3.2% | **5.6%** |
| `$1000–1500` | **19.7%** | **4.8%** |
| `$1500–2000` | 6.1% | **4.1%** |
| `$2000–2500` | 0.8% | **3.5%** |
| `$2500–5000` | 0.5% | **3.0%** |
| `$5k+` / max | как publish | без изменений |

Каскад у dog длиннее, чем у cat: средний win выше (~`$574`), иначе EV не сойдётся без второго пика.

### Инварианты (оба режима)

- RTP ≈ `0.96`, max `×25000`, soft `×2500`
- Cat ~50% / dog ~25% win-rate
- Нет ничьи; квант `0.1×`; `assert_duel_invariants`
- Каскад величины победы SMOOTH · VH — **и** среди wins (`payout`), **и** среди loses (`victoryAmount`)

---

## Тема A — Интрига (**math**, после accept B)

Реальные банки **оба** копится; бой близко до конца; UI показывает правду. Старт после B.

Проигрыш в A усиливает B.0.1: ощущение конкуренции / «чуть не хватило», а не сухой blowout в ноль у игрока.

Кандидат №1: post-pass pad лузера с сохранением winner + целевого payout / pot из B.

---

## Общий порядок работ

```
1) ✅ Зафиксировать целевую форму cat + dog (SMOOTH · VH)
2) Реализовать B/cat + B/dog в math → M5 → accept
3) Тема A → M5 + регресс B
4) M6 + resample перед Stake publish
```

### Критерии «готово» B/cat

- Каскад тела визуально/по % совпадает с TARGET (± допуск) **среди wins и среди loses**
- P(`<$150` \| win) ≈ 2%; то же по pot среди loses
- Горб `$350–450` снят (не доминирует)
- RTP и P(win) в допуске; max hit rate не сломан

---

## Следующий шаг

1. Реализация по `CatMafia_Duel_SMOOTH_VH_Implementation_Plan.md`: сначала **фаза B**, затем **фаза A** (math intrigue).
2. UI-фейк банков не делаем.
